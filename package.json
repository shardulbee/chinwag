{
  "name": "chinwag",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "scripts/build-service && scripts/build-app",
    "check": "scripts/check",
    "format": "gofmt -w cmd internal",
    "install:mac": "scripts/service install",
    "serve": "scripts/build-service && scripts/service run",
    "status": "scripts/service status",
    "uninstall:mac": "scripts/service uninstall"
  },
  "dependencies": {
    "@transcribe-cpp/darwin-arm64-metal": "0.2.1"
  }
}
